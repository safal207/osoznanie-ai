# Decision traces

Decision traces record the exact policy and memory identifiers used for an agent action. IDs are deterministic, records are immutable, and later outcomes create a superseding version rather than modifying history.

Private model reasoning is not stored.
